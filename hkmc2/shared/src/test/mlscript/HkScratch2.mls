:js
// :de
// :sjs
// :pt
// :elt

:global
// :d
// :todo


object Bar with
  fun x = 1
  x

// :lot
:dl
:ssjs
module Bar with
  // fun foo = 1
  fun x = 1
  x
//│ Lowering.term ( () )
//│ Lowering.term ()
//│ Lowering.term 1
//│ Lowering.term ( (module:Bar#666.)x‹member:x› )
//│ Lowering.term (module:Bar#666.)x‹member:x› – symbol member:x
//│ Expanding term SynthSel(Ref(module:Bar),Ident(x))
//│ ~> (module:Bar#666.)x‹member:x›
//│ Lowering.term module:Bar#666 – symbol module:Bar
//│ Expanding term Ref(module:Bar)
//│ ~> module:Bar#666
//│ Lowering.term undefined
//│ avoidNames Define(ClsLikeDefn(None,class:Bar,member:Bar,Cls,None,List(),None,List(),List(),List(),End(),Return(...
//│ | avoidNames End()...
//│ | avoidNames Return(Select(Ref($runtime),Ident(Unit)),true)...
//│ | avoidNames Return(Select(Ref(module:Bar),Ident(x)),true)...
//│ | avoidNames Return(Lit(IntLit(1)),false)...
//│ | avoidNames Assign($block$res,Lit(UnitLit(false)),End())...
//│ | | avoidNames End()...
//│ JS:
//│ globalThis.Object.freeze(class Bar2 {
//│   static {
//│     Bar3 = this
//│   }
//│   constructor() {
//│     runtime.Unit;
//│   }
//│   static {
//│     Bar2.x;
//│   }
//│   static get x() {
//│     return 1;
//│   }
//│   toString() { return runtime.render(this); }
//│   static [definitionMetadata] = ["class", "Bar"]; 
//│ });
//│ block$res2 = undefined;

Bar.x
//│ = 1


// fun makeLetBindings(x) = [x]
// fun rule(x, y) = [x, y]

// let letExpression = makeLetBindings(true)
// //│ letExpression = [true]

// rule of
//   "prefix rules for expressions"
//   letExpression
// //│ = ["prefix rules for expressions", [true]]

// // Prefix rules and infix rules for expressions.
// val termRule = rule of
//   "prefix rules for expressions"
//   letExpression
// //│ termRule = ["prefix rules for expressions", [true]]


// :elt
// :dl
// :ssjs
// :lot
module Foo with
  let x = 1
  print(x)
//│ > 1


// :ssjs
// :elt
// :lot
module Foo with
  class CollapsibleTree
  print of CollapsibleTree
//│ > class CollapsibleTree


// :ssjs
// :elt
// :lot
module Foo with
  mut val x = 1




