:js

// Instruction for agent: for each test case you solve, move it to its own test file outside of the `backlog` folder

// ——— ——— ———

let { x = 0, y = 0 }
//│ x = 0
//│ y = 0

// Works:
if false then 0
else
  set { x = 1, y = 1 }

// Does not work:
:fixme
if false then 0
else set { x = 1, y = 1 }
//│ ╔══[PARSE ERROR] Unexpected comma here
//│ ║  l.19: 	else set { x = 1, y = 1 }
//│ ╙──      	                ^

// ——— ——— ———
