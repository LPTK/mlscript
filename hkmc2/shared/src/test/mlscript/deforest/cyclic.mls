:js

data class A(x)
data class B(x)
data object C
data object D
data class (::) Cons(h, t)
object Nil
object None
data class Some(x)



:expect 0
fun f(x) = if x is
  A(_) then f(A(2))
  else 0
f(Nil)
//│ = 0


:expect 0
fun f(x) =
  let res = if x is
    A(x) then x
  if res > 0 then f(A(res - 1)) else 0
f(A(4))
//│ = 0


:expect 0
fun f(x, b) = if x is
  Nil then if b then f(Nil, false) else 0
f(Nil, false)
//│ = 0



:deforest
fun f(a) = if a is
  C then C
  else D
(x => 0)(f(f(C)))
//│ deforest > >>> fusing >>>
//│ deforest > C⁰ ->
//│ deforest > 	match: a⁰
//│ deforest > D⁰ ->
//│ deforest > 	match: a⁰
//│ deforest > C⁰ ->
//│ deforest > 	match: a⁰
//│ deforest > <<< fusing <<<
//│ = 0


:deforest
:expect Some(2)
fun maxOpt(ls) = if ls is
  Nil then None
  h :: t then
    if maxOpt(t) is
      None then Some(h)
      Some(m) then if m > h then Some(m) else Some(h)
maxOpt(id(1 :: 2 :: Nil))
//│ deforest > >>> fusing >>>
//│ deforest > <<< fusing <<<
//│ = Some(2)


:deforest
:sir
:expect Some(2)
fun modSome(x, m) = if x is
  Some(n) and
    n >= m then modSome(Some(n - m), m)
    else Some(n)
  None then None
modSome(Some(17), 3)
//│ deforest > >>> fusing >>>
//│ deforest > Some⁰(tmp⁰) ->
//│ deforest > 	match: x⁰
//│ deforest > 	fields: x⁰.x¹
//│ deforest > Some⁰(17) ->
//│ deforest > 	match: x⁰
//│ deforest > 	fields: x⁰.x¹
//│ deforest > <<< fusing <<<
//│ ———————————————| Lowered IR |———————————————————————————————————————————————————————————————————————
//│ let modSome⁰, tmp, modSome_12$modSome⁰, modSome_12$x_Some⁰, modSome_12$x_rest⁰, Some_x;
//│ @private
//│ define modSome_12$modSome⁰ as fun modSome_12$modSome¹(x, m) {
//│   return x(m)
//│ };
//│ @affine(1) @private
//│ define modSome_12$x_Some⁰ as fun modSome_12$x_Some¹(Some_x1)(fv_m) {
//│   let n, scrut, arg$Some$0$;
//│   set arg$Some$0$ = Some_x1;
//│   set n = arg$Some$0$;
//│   set scrut = >=⁰(n, fv_m);
//│   match scrut
//│     true =>
//│       let tmp1, tmp2, Some_x2;
//│       set tmp1 = -⁰(n, fv_m);
//│       set Some_x2 = tmp1;
//│       set tmp2 = modSome_12$x_Some¹(Some_x2);
//│       return modSome_12$modSome¹(tmp2, fv_m)
//│     else
//│       return Some⁰(n)
//│   end
//│ };
//│ @private
//│ define modSome_12$x_rest⁰ as fun modSome_12$x_rest¹() {
//│   return null
//│ };
//│ define modSome⁰ as fun modSome¹(x, m) {
//│   loop loopLabel:
//│     match x
//│       Some¹ =>
//│         let n, scrut, arg$Some$0$;
//│         set arg$Some$0$ = x.x¹;
//│         set n = arg$Some$0$;
//│         set scrut = >=⁰(n, m);
//│         match scrut
//│           true =>
//│             let tmp1, tmp2;
//│             set tmp1 = -⁰(n, m);
//│             set tmp2 = Some⁰(tmp1);
//│             set x = tmp2;
//│             continue loopLabel
//│           else
//│             return Some⁰(n)
//│         end
//│       None⁰ =>
//│         return None⁰
//│       else
//│         throw new globalThis⁰.Error⁰("match error")
//│     end
//│   end
//│ };
//│ set Some_x = 17;
//│ set tmp = modSome_12$x_Some¹(Some_x);
//│ return modSome_12$modSome¹(tmp, 3)
//│ —————————————————| Output |—————————————————————————————————————————————————————————————————————————
//│ = Some(2)


:deforest
:expect Some(3)
fun maxOpt2(ls, x) = if ls is
  Nil then x
  h :: t and x is
    None then maxOpt2(t, Some(h))
    Some(m) and
      h > m then maxOpt2(t, Some(h))
      else maxOpt2(t, Some(m))
maxOpt2(id(1 :: 2 :: 3 :: Nil), None)
//│ deforest > >>> fusing >>>
//│ deforest > <<< fusing <<<
//│ = Some(3)

:deforest
:expect Nil
fun zipWith(xsys, f) = if xsys is
  [hx :: tx, hy :: ty] then f(hx, hy) :: zipWith([tx, ty], f)
  [Nil, Nil] then Nil
zipWith([id(Nil), id(Nil)], (x, y) => x)
//│ deforest > >>> fusing >>>
//│ deforest > [tx⁰, ty⁰] ->
//│ deforest > 	match: xsys⁰
//│ deforest > 	fields: runtime⁰.Tuple﹖.get⁰(xsys⁰, 0)
//│ deforest > 	fields: runtime⁰.Tuple﹖.get⁰(xsys⁰, 1)
//│ deforest > [tmp¹, tmp²] ->
//│ deforest > 	match: xsys⁰
//│ deforest > 	fields: runtime⁰.Tuple﹖.get⁰(xsys⁰, 0)
//│ deforest > 	fields: runtime⁰.Tuple﹖.get⁰(xsys⁰, 1)
//│ deforest > <<< fusing <<<
//│ = Nil
