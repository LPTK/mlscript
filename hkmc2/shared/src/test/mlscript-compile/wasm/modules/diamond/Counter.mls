#config(target: CompilationTarget.Wasm)

// Both branches must share this storage and initialize it only once.
module Counter with
  class Cell(val value)
  class Other(val value)
  let count = 0
  fun next() =
    set count = count + 1
    count
